# Симулятор (цифровой двойник) аппарата.

Управляется WASD, крутится и зумится мышью.

Запускается локальный websocket сервер, который стримит изображение. См. `ws_client.py` для перекодировки и трансляции по rtsp.
